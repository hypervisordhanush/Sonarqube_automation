# Environment variables are now expected to be provided at runtime via the command line or Docker --env/--env-file options.
# Example usage:
#   docker run -e SONARQUBE_URL=... -e SONARQUBE_TOKEN=... -e PROJECT_KEY=... -e GITHUB_TOKEN=... -e GITHUB_REPO=... -e GITHUB_USER=... sonarqube-mcp
